global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: "workers" # scraping worker metrics
    dns_sd_configs:
      - names: ["worker"]
        type: A
        port: 9100
